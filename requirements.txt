# Core (also declared in pyproject.toml -- prefer `pip install .`)
numpy>=1.24
soundfile>=0.12.1
torch>=2.0
pyannote.audio>=3.1
huggingface_hub>=0.23

# Optional: transcription (`pip install ".[transcribe]"`)
# faster-whisper>=1.0

# ffmpeg / ffprobe must be on PATH (not a pip package):
#   Debian/Ubuntu: sudo apt install ffmpeg
#   macOS:         brew install ffmpeg
